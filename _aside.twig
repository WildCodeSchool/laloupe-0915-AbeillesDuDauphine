
<aside class="large-3 columns">

        <h5><u>Actualités</u></h5>
        {% setcontent records = "pages/latest/5" %}
        <ul>
        {% for record in records %}
          <li><a href="{{ record.link }}">{{ record.title }}</a></li>
        {% endfor %}
          <p><a href="{{ paths.root }}pages"><br><i>Plus d'articles...</i></br></a></p>
        <ul>

</aside>

